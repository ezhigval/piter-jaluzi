# Jaluxi - Сайт по изготовлению и ремонту жалюзи

Современный сайт для компании Jaluxi с функционалом заказа, расчета стоимости и управления контентом.

## 🚀 Функционал

- **Каталог материалов** с фильтрацией и расчетом стоимости
- **Минималистичная анимация** загрузки в виде открывающихся жалюзи
- **Админ-панель** с авторизацией для управления контентом
- **Управление материалами** - добавление/удаление
- **Управление отзывами** с ответами компании
- **Визуальная формула** ценообразования
- **Адаптивный дизайн** для всех устройств
- **Современный UI** в slate цветовой палитре

## 🛠️ Технологии

- **Frontend:** Next.js 14, TypeScript, Tailwind CSS
- **Backend:** Go, Gin framework
- **Анимация:** CSS transitions, React hooks
- **Хранение:** Cookies, SessionStorage
- **Авторизация:** Cookie-based сессии

## 📦 Установка и запуск

### Frontend

```bash
cd web
npm install
```

Создайте файл `.env.local`:
```env
ADMIN_PASSWORD=admin
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
```

Запустите development сервер:
```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

### Backend

```bash
cd backend
go mod tidy
go run main.go
```

Backend будет доступен на [http://localhost:8080](http://localhost:8080)

## 🔐 Доступ к админ-панели

1. Перейдите на [http://localhost:3000/login](http://localhost:3000/login)
2. Введите пароль: `admin` (или тот, что указан в `.env.local`)
3. Доступ к управлению материалами, отзывами и ценообразованием

## 📁 Структура проекта

```
web/
├── src/
│   ├── app/                 # Next.js App Router
│   │   ├── admin/          # Админ-панель
│   │   ├── api/            # API routes
│   │   ├── catalog/        # Каталог материалов
│   │   ├── repair/         # Ремонт жалюзи
│   │   └── ...
│   ├── components/         # React компоненты
│   │   ├── BlindsLoader.tsx
│   │   ├── Footer.tsx
│   │   └── AdminAuth.tsx
│   ├── hooks/              # Custom hooks
│   │   ├── useAnimation.ts
│   │   └── ...
│   ├── lib/                # Утилиты и API
│   │   ├── api.ts
│   │   └── storage/        # Модульная система хранения
│   └── ...
└── public/
    └── images/
        └── materials/      # Изображения материалов
```

## 🎨 Дизайн

- **Цветовая палитра:** Slate (серые тона)
- **Шрифты:** Inter (Google Fonts)
- **Стиль:** Минимализм, интерьерный дизайн
- **Анимации:** Плавные переходы, hover эффекты

## ⚙️ Особенности реализации

### Система анимации
- Показывается при первом посещении (через cookies)
- Показывается при обновлении страницы (F5)
- Не показывается при навигации внутри сайта
- Модульная структура хранения в `/src/lib/storage/`

### Админ-панель
- Авторизация через cookies
- Управление материалами (CRUD)
- Управление отзывами с ответами компании
- Визуальная формула ценообразования
- Современный UI в едином стиле

### Каталог
- Фильтрация по категории, цвету, цене
- Адаптивная сетка карточек
- Hover эффекты с масштабированием изображений
- Форма расчета стоимости

## 📱 Адаптивность

- **Mobile-first подход**
- **Липкий хедер** с backdrop-blur
- **Адаптивные карточки** и формы
- **Оптимизированные изображения**

## 🚀 Развертывание

### Vercel (рекомендуется)
```bash
npm run build
```

Разверните на Vercel с автоматическими переменными окружения.

### Docker
```bash
docker build -t jaluxi-web .
docker run -p 3000:3000 jaluxi-web
```

## 📝 Лицензия

MIT License
