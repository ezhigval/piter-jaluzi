# Руководство по деплою с персистентностью

## Что исправлено

✅ **Проблема 1: Данные терялись при перезапуске**
- Заменен `inMemoryStore` на `persistentStore`
- Все данные теперь сохраняются в JSON файлы в директории `/app/data`
- Автосохранение происходит после каждого изменения через админку

✅ **Проблема 2: Подписчики Telegram терялись при деплое**
- Подписчики теперь хранятся в файле `telegram-subscribers.json`
- Добавлены API эндпоинты для управления подписчиками
- Фронтенд обновлен для использования бэкенд API

## Структура данных

```
/app/data/
├── materials.json           # Каталог материалов
├── promotions.json         # Акции и предложения
├── reviews.json           # Отзывы клиентов
├── pricing.json           # Настройки ценообразования
├── site-content.json      # Контент страниц
├── site-config.json       # Конфигурация сайта
├── telegram-subscribers.json # Подписчики Telegram
└── next-id.json          # Генератор ID
```

## Изменения в файлах

### Backend (`main.go`)
- Новый тип `persistentStore` вместо `inMemoryStore`
- Методы `loadData()` и `saveData()` для JSON файлов
- Автосохранение через `go s.saveData()` после изменений
- Новые API эндпоинты для управления подписчиками Telegram

### Dockerfile
- Добавлена переменная `DATA_DIR=/app/data`
- Добавлен volume `VOLUME ["/app/data"]`
- Копируются начальные данные из `backend/data`

### Render (`render.yaml`)
- Добавлен persistent disk на 1GB
- Mount path: `/app/data`
- Переменная окружения `DATA_DIR=/app/data`

### Frontend
- Обновлен `telegram-subscribers.ts` для использования API
- Добавлены асинхронные функции для работы с подписчиками

## Процесс деплоя

1. **Зафиксируйте изменения:**
```bash
git add .
git commit -m "Add persistent storage for all data including Telegram subscribers"
git push
```

2. **Render автоматически:**
- Соберет новый Docker образ
- Создаст persistent disk (если еще не создан)
- Смонтирует диск в `/app/data`
- Запустит приложение с загрузкой данных из файлов

3. **Первый запуск:**
- Данные загрузятся из JSON файлов в `backend/data`
- При последующих деплоях данные будут загружаться из persistent disk

## Проверка работоспособности

### 1. Проверка персистентности данных:
```bash
# Зайдите в админку /admin
# Измените любой материал или настройку ценообразования
# Перезапустите деплой (push в git)
# Данные должны сохраниться
```

### 2. Проверка подписчиков Telegram:
```bash
# Откройте Telegram бот
# Нажмите /start и подпишитесь на уведомления
# Отправьте тестовую заявку с сайта
# Перезапустите деплой
# Подписка должна сохраниться
```

### 3. Проверка API:
```bash
# Получить список подписчиков
curl https://your-app.onrender.com/api/telegram/subscribers

# Добавить подписчика
curl -X POST https://your-app.onrender.com/api/telegram/subscribers/123456789
```

## Важные замечания

1. **Render Free Plan**: 1GB disk достаточно для JSON файлов
2. **Бэкапы**: Данные теперь можно бэкапить через скачивание JSON файлов
3. **Восстановление**: Просто замените JSON файлы в `/app/data`
4. **Миграция**: Если нужно сбросить данные, удалите файлы на диске

## Мониторинг

Проверьте логи после деплоя:
```
Warning: could not load materials: file not found
Warning: could not load telegram subscribers: file not found
```

Эти предупреждения нормальны при первом запуске - система создаст файлы с данными по умолчанию.

## Резервное копирование

Для бэкапа данных можно добавить эндпоинт:
```go
// Добавить в registerRoutes()
r.Get("/admin/backup", a.handleBackup())

func (a *App) handleBackup() http.HandlerFunc {
    return func(w http.ResponseWriter, r *http.Request) {
        // Архивировать все JSON файлы и отдать как download
    }
}
```

Теперь все данные сохраняются между деплоями! 🎉
