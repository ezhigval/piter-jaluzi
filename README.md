# Северный Контур - Профессиональный сайт компании жалюзи

## 📋 Обзор проекта

Современный full-stack сайт для компании по производству и установке жалюзи с удобной админ-панелью управления контентом.

### 🎯 Основной функционал

**Для клиентов:**
- Главная страница с презентацией услуг
- Каталог жалюзи с конструктором расчета стоимости
- Страница ремонта и обслуживания
- Информация о компании с интерактивной картой
- Отзывы клиентов с формой добавления
- Фотогалерея выполненных работ

**Для администратора:**
- Полноценная админ-панель управления
- Редактирование страниц и блоков контента
- Управление каталогом материалов и цен
- Модерация отзывов и ответов
- Управление фотогалереей
- Настройка SEO и мета-данных
- Интеграция с Telegram и почтой

## 🏗️ Архитектура проекта

```
SITE_JALUXI/
├── frontend/                 # React приложение
│   ├── src/
│   │   ├── components/      # Переиспользуемые компоненты
│   │   ├── pages/         # Страницы приложения
│   │   ├── hooks/         # Кастомные хуки
│   │   ├── services/      # API сервисы
│   │   ├── utils/         # Утилиты и хелперы
│   │   ├── types/         # TypeScript типы
│   │   └── styles/       # Стили и темы
│   ├── public/
│   └── package.json
├── backend/                  # Go приложение
│   ├── cmd/               # Точки входа приложения
│   ├── internal/          # Внутренние пакеты
│   │   ├── api/          # API handlers
│   │   ├── service/      # Бизнес-логика
│   │   ├── repository/   # Работа с БД
│   │   ├── models/       # Модели данных
│   │   └── middleware/   # Middleware
│   ├── migrations/        # Миграции БД
│   ├── configs/          # Конфигурации
│   └── go.mod
├── shared/                  # Общие типы и утилиты
│   ├── types/
│   └── utils/
├── docker/                  # Docker конфигурации
├── docs/                    # Документация
└── scripts/                 # Скрипты сборки и деплоя
```

## 🛠️ Технологический стек

### Frontend
- **React 18** - UI библиотека
- **TypeScript** - Типизация
- **Vite** - Сборщик (быстрее Webpack)
- **Tailwind CSS** - CSS фреймворк
- **Framer Motion** - Анимации
- **React Query** - Управление состоянием сервера
- **React Hook Form** - Формы
- **Zod** - Валидация данных

### Backend
- **Go 1.21** - Язык программирования
- **Gin** - Web фреймворк
- **GORM** - ORM для PostgreSQL
- **PostgreSQL** - База данных
- **Redis** - Кэширование и сессии
- **JWT** - Аутентификация

### DevOps
- **Docker** - Контейнеризация
- **Docker Compose** - Локальная разработка
- **GitHub Actions** - CI/CD
- **Render** - Хостинг

## 🔒 Безопасность

- JWT токены с refresh механизмами
- Rate limiting для API
- Валидация всех входных данных
- SQL injection защита через GORM
- CORS настройки
- Хеширование паролей (bcrypt)
- HTTPS только
- Защита от XSS и CSRF

## 📊 Производительность

- Ленивая загрузка изображений
- Кэширование Redis
- Оптимизированные SQL запросы
- Сжатие gzip/brotli
- CDN для статики
- Индексация БД

## 🚀 Развертывание

### Локальная разработка
```bash
docker-compose up -d
```

### Продакшн
```bash
# Автоматический деплой через GitHub Actions
git push main
```

## 📱 Адаптивность

- Mobile-first подход
- Breakpoints: 320px, 768px, 1024px, 1440px
- Touch-friendly интерфейсы
- Оптимизация для мобильных устройств

## 🎨 Дизайн система

- Цветовая палитра бренда
- Типографика
- Компоненты UI
- Анимации и переходы
- Темная/светлая тема (опционально)

## 📈 Мониторинг

- Логирование ошибок
- Метрики производительности
- Uptime мониторинг
- Аналитика поведения пользователей

## 🧪 Тестирование

- Unit тесты (Go)
- Интеграционные тесты
- E2E тесты (Playwright)
- TypeScript для типизации

## 📝 Документация

- API документация (OpenAPI)
- Руководство разработчика
- Пользовательская документация
- Архитектурные решения

---

**Разработано с любовью к деталям и профессиональным подходом** ❤️

## Запуск локально

### 1. Frontend

```bash
cd web
npm install        # один раз
npm run dev
```

Приложение будет доступно на `http://localhost:3000`. Для продакшн‑бандла:

```bash
npm run build
```

Статический экспорт лежит в `web/out` и используется Go‑сервером.

### 2. Backend

```bash
cd backend
go run .
```

По умолчанию сервер слушает `http://localhost:8080` и:

- отдает статику Next (если есть `web/out`)
- обрабатывает API под `/api/*`

Основные эндпоинты:

- `GET /api/health` — health‑check
- `GET /api/catalog` — каталог материалов (локальная копия)
- `POST /api/estimate` — расчет ориентировочной стоимости
- `GET /api/promotions` — акции
- `GET /api/reviews`, `POST /api/reviews` — отзывы

## Деплой на Render

1. Залить репозиторий на GitHub.
2. В Render создать **Web Service**, выбрать репозиторий.
3. Render автоматически подхватит `render.yaml` и `Dockerfile`.
4. После билда сервис будет доступен по одному URL; фронт и API живут в одном контейнере.

## Дальнейшее развитие

- Подключить PostgreSQL и вынести каталог/отзывы/акции в БД.
- Добавить периодическую синхронизацию с сайтом поставщика (`intersklad.ru`) и административную панель.
- Включить антиспам (reCAPTCHA/Turnstile) на формах отзывов и заявок.

