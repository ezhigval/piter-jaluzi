# Frontend - Северный Контур

## 📋 Обзор

Современный frontend на React 18 с Vite для сайта компании жалюзи "Северный Контур".

## 🛠️ Технологический стек

- **React 18** - UI библиотека с хуками
- **Vite** - Быстрый сборщик и dev сервер
- **JavaScript** - Для простоты и скорости разработки
- **React Router** - Клиентская навигация
- **Axios** - HTTP клиент для API запросов
- **Tailwind CSS** - Utility-first CSS фреймворк
- **ESLint** - Линтинг и форматирование кода

## 🏗️ Структура проекта

```
frontend/
├── src/
│   ├── components/          # Переиспользуемые компоненты
│   │   ├── ui/         # Базовые UI компоненты
│   │   │   ├── Button.jsx
│   │   │   └── Input.jsx
│   │   └── ...         # Другие компоненты
│   ├── pages/             # Страницы приложения
│   │   ├── Home.jsx      # Главная страница
│   │   └── ...          # Другие страницы
│   ├── services/          # API сервисы
│   │   └── api.js       # API клиент
│   ├── types/             # Типы данных
│   │   └── index.js     # Основные типы
│   ├── App.jsx            # Корневой компонент
│   ├── main.jsx           # Точка входа
│   └── index.html         # HTML шаблон
├── public/                # Статические файлы
├── package.json           # Зависимости и скрипты
├── vite.config.js         # Конфигурация Vite
├── nginx.conf            # Конфигурация Nginx
└── Dockerfile.frontend    # Docker конфигурация
```

## 🚀 Запуск проекта

### Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр сборки
npm run preview

# Линтинг
npm run lint

# Исправление линтинга
npm run lint:fix
```

### Docker

```bash
# Запуск с Docker Compose
docker-compose up frontend

# Сборка Docker образа
docker build -f Dockerfile.frontend -t jaluxi-frontend .
```

## 📱 Страницы приложения

### Главная страница (`/`)
- Hero секция с призывом к действию
- Каталог продукции с категориями
- Отзывы клиентов
- Форма заявки
- Футер с контактами

### Каталог (`/catalog`)
- Фильтрация по категориям
- Сетка материалов
- Детальная информация о каждом материале
- Расчет стоимости

### Отзывы (`/reviews`)
- Список опубликованных отзывов
- Форма добавления нового отзыва
- Рейтинги и оценки

### Галерея (`/gallery`)
- Фотографии выполненных работ
- Категоризация по типам жалюзи
- Модальное окно для просмотра

### Контакты (`/contacts`)
- Контактная информация
- Интерактивная карта
- Форма обратной связи

## 🎨 Дизайн система

### Цветовая палитра
- **Primary**: Blue 600 (#2563EB)
- **Secondary**: Gray 200 (#E5E7EB)
- **Accent**: Blue 700 (#1D4ED8)
- **Text**: Gray 900 (#111827)
- **Background**: Gray 50 (#F9FAFB)

### Типографика
- **Шрифты**: Inter (Google Fonts)
- **Размеры**: Mobile-first подход
- **Breakpoints**: 320px, 768px, 1024px, 1440px

### Компоненты
- **Button**: primary, secondary, outline варианты
- **Input**: с валидацией и ошибками
- **Forms**: React Hook Form
- **Layout**: Адаптивная сетка

## 🔗 API интеграция

### Эндпоинты
- `GET /api/v1/categories` - Получение категорий
- `GET /api/v1/categories/:slug/materials` - Материалы категории
- `GET /api/v1/reviews` - Отзывы
- `POST /api/v1/reviews` - Добавление отзыва
- `POST /api/v1/leads` - Создание заявки
- `GET /api/v1/config` - Конфигурация сайта

### Пример использования
```javascript
import { categoriesApi, leadsApi } from '../services/api'

// Получение категорий
const categories = await categoriesApi.getAll()

// Создание заявки
const lead = await leadsApi.create({
  name: 'Иван Иванов',
  phone: '+7 (812) 123-45-67',
  windowWidth: 200,
  windowHeight: 150,
  message: 'Хочу заказать жалюзи'
})
```

## 🎯 Особенности

### 🚀 Производительность
- **Lazy loading** изображений
- **Code splitting** по роутам
- **Кэширование** API запросов
- **Оптимизация** бандла

### 📱 Адаптивность
- **Mobile-first** дизайн
- **Touch-friendly** интерфейсы
- **Responsive** сетки
- **PWA** готовность

### 🔒 Безопасность
- **CSRF** защита
- **XSS** предотвращение
- **Content Security Policy**
- **HTTPS** только в продакшене

## 🧪 Тестирование

```bash
# Линтинг
npm run lint

# Типизация
npm run type-check

# E2E тесты (в планах)
npm run test
```

## 📦 Сборка и деплой

### Продакшен
```bash
# Сборка
npm run build

# Результат
dist/
├── index.html
├── assets/
│   ├── index-abc123.js
│   └── index-def456.css
└── ...
```

### Docker
```bash
# Multi-stage сборка
FROM node:20-alpine AS builder
# ... сборка
FROM nginx:alpine
# ... продакшен
```

## 🔧 Конфигурация

### Vite
- **Dev сервер**: http://localhost:3000
- **API прокси**: http://localhost:8080/api/v1
- **Hot reload**: Автоматический
- **Source maps**: Включены

### Nginx
- **Gzip сжатие**: Включено
- **Кэширование статики**: 1 год
- **Security headers**: Настроены
- **SPA fallback**: index.html

## 📈 Мониторинг

### Метрики
- **Core Web Vitals**: Автоматический сбор
- **Performance Budget**: Настраивается
- **Bundle Analysis**: Анализ размера

### Логирование
- **Error tracking**: Sentry (в планах)
- **User analytics**: Яндекс.Метрика
- **Performance**: Lighthouse

## 🎨 Дизайн

### UI/UX принципы
- **Consistency**: Единый дизайн
- **Accessibility**: WCAG 2.1 AA
- **Performance**: < 3с загрузка
- **Mobile**: Touch-friendly

### Анимации
- **Micro-interactions**: Hover, focus
- **Page transitions**: React Router
- **Loading states**: Skeleton screens
- **Error states**: Graceful fallbacks

---

**Разработано с любовью к деталям и фокусом на пользовательском опыте** ❤️
