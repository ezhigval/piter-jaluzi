# Backend API для сайта "Северный Контур"

Backend API для сайта по производству и установке жалюзи с использованием Go, Gin, GORM и PostgreSQL.

## 🚀 Технологии

- **Go 1.21** - Основной язык программирования
- **Gin** - HTTP фреймворк
- **GORM v2** - ORM для работы с PostgreSQL
- **PostgreSQL** - Основная база данных
- **Docker** - Контейнеризация
- **Air** - Hot reload для разработки

## 📁 Структура проекта

```
backend/
├── cmd/
│   └── server/
│       └── main.go          # Точка входа приложения
├── configs/
│   └── config.go           # Конфигурация приложения
├── internal/
│   ├── database/
│   │   └── database.go     # Работа с базой данных
│   ├── handler/
│   │   └── category_handler.go  # HTTP обработчики
│   ├── models/
│   │   └── models.go       # Модели данных
│   ├── repository/
│   │   ├── category.go      # Repository для категорий
│   │   └── material.go      # Repository для материалов
│   ├── routes/
│   │   └── category_routes.go  # Маршруты API
│   └── service/
│       └── category_service.go  # Бизнес-логика
├── Dockerfile.dev           # Docker для разработки
├── docker-compose.dev.yml   # Docker Compose для разработки
├── .air.toml               # Конфигурация Air
├── Makefile                # Удобные команды
└── README.md               # Этот файл
```

## 🛠️ Установка и запуск

### Локальная разработка

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/ezhigval/piter-jaluzi.git
   cd piter-jaluzi/backend
   ```

2. **Установите зависимости:**
   ```bash
   make deps
   make install-air
   ```

3. **Настройте переменные окружения:**
   ```bash
   cp .env.example .env
   # Отредактируйте .env файл
   ```

4. **Запустите PostgreSQL:**
   ```bash
   make docker-up
   ```

5. **Запустите приложение с hot reload:**
   ```bash
   make dev
   ```

### Docker разработка

1. **Запустите все сервисы:**
   ```bash
   make docker-up
   ```

2. **Просмотрите логи:**
   ```bash
   make docker-logs
   ```

3. **Остановите сервисы:**
   ```bash
   make docker-down
   ```

## 📊 Модели данных

### User (Пользователи)
- Администраторы системы
- Аутентификация через JWT

### Category (Категории)
- Типы жалюзи (горизонтальные, вертикальные, рулонные, плиссе)
- SEO поля для каждой категории

### Material (Материалы)
- Характеристики материалов
- Цены и наличие
- Поставщики

### Page (Страницы)
- Динамические страницы сайта
- SEO оптимизация
- Навигация

### Review (Отзывы)
- Отзывы клиентов
- Модерация

### Lead (Заявки)
- Заявки от клиентов
- Статусы обработки

## 🔌 API Эндпоинты

### Категории
- `GET /api/v1/categories` - Все категории
- `GET /api/v1/categories/:id` - Категория по ID
- `GET /api/v1/categories/slug/:slug` - Категория по slug
- `GET /api/v1/categories/slug/:slug/materials` - Категория с материалами
- `POST /api/v1/categories` - Создать категорию (admin)
- `PUT /api/v1/categories/:id` - Обновить категорию (admin)
- `DELETE /api/v1/categories/:id` - Удалить категорию (admin)

### Health Check
- `GET /health` - Проверка состояния сервера

## 📝 Примеры запросов

### Получить все категории
```bash
curl http://localhost:8080/api/v1/categories
```

### Получить категорию с материалами
```bash
curl http://localhost:8080/api/v1/categories/slug/horizontal/materials
```

### Создать категорию (admin)
```bash
curl -X POST http://localhost:8080/api/v1/categories \
  -H "Content-Type: application/json" \
  -u admin:admin123 \
  -d '{
    "name": "Римские",
    "slug": "roman",
    "description": "Римские шторы",
    "order": 5,
    "is_active": true
  }'
```

## 🧪 Тестирование

```bash
# Запустить все тесты
make test

# Запустить тесты с покрытием
go test -v -cover ./...

# Запустить конкретный тест
go test -v ./internal/service
```

## 🔧 Конфигурация

### Переменные окружения

```bash
# Сервер
SERVER_HOST=localhost
SERVER_PORT=8080
GIN_MODE=debug

# База данных
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres123
DB_NAME=jaluxi
DB_SSLMODE=disable

# JWT
JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRATION=24h
```

## 📦 Сборка

```bash
# Собрать приложение
make build

# Собрать для продакшена
CGO_ENABLED=0 GOOS=linux go build -o main ./cmd/server
```

## 🚀 Деплой

### Docker
```bash
# Собрать образ
docker build -t jaluxi-backend .

# Запустить
docker run -p 8080:8080 jaluxi-backend
```

### Render.com
Проект настроен для автоматического деплоя на Render.com через `render.yaml`.

## 🔒 Безопасность

- Basic Auth для административных эндпоинтов
- Валидация входных данных
- CORS настройки
- Environment variables для чувствительных данных

## 📈 Мониторинг

- Health check endpoint
- Структурированное логирование
- Graceful shutdown

## 🤝 Контрибьюция

1. Fork проекта
2. Создайте feature branch
3. Сделайте коммиты
4. Отправьте Pull Request

## 📄 Лицензия

MIT License

## 📞 Контакты

- Email: info@severnyj-kontur.ru
- Телефон: +7 (812) 123-45-67
- Сайт: https://severnyj-kontur.ru
